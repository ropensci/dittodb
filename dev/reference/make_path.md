# make a mock path

make a mock path

## Usage

``` r
make_path(path, type, hash)
```

## Arguments

- path:

  the path to look in

- type:

  what type of query is it? (e.g. `SELECT`, `INSERT`)

- hash:

  the hash of the query

## Value

a constructed path to a mock
