# Make a (short) hash from a string

Make a (short) hash from a string

## Usage

``` r
hash(string, n = 6)
```

## Arguments

- string:

  the string to hash

- n:

  how long should the hash be? (default: 6)

## Value

a hash for the string
